# Control-node tooling for Shackwright development and CI.
#
# This is what you need on the machine that RUNS the playbook, not on the
# Raspberry Pi being built. Install with:
#
#     python3 -m pip install -r requirements.txt
#
# Debian and Ubuntu mark their system Python as externally managed (PEP 668),
# so prefer a virtual environment:
#
#     python3 -m venv ~/.venv/shackwright
#     ~/.venv/shackwright/bin/pip install -r requirements.txt
#
ansible-core>=2.16,<2.22
ansible-lint>=24.2
yamllint>=1.35
